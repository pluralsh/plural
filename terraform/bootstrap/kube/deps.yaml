dependencies:
- name: gcp-bootstrap
  repo: bootstrap
  type: terraform
providers:
- gcp
description: bootstrapping gcp credentials
version: 0.1.0

wirings:
  terraform:
    cluster_name: gcp-bootstrap.cluster_name
    node_pool: gcp-bootstrap.node_pool