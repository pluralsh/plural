#!/bin/sh
set -eu
cd -P -- "$(dirname -- "$0")"
if [ "$#" -eq 0 ]; then
  exec ./plural eval "Piazza.Ecto.ReleaseTasks.seed([])"
fi
quoted=$(printf '"%s",' "$@" | sed 's/,$//')
exec ./plural eval "Piazza.Ecto.ReleaseTasks.seed([${quoted}])"
