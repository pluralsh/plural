apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: slashcommands.forgelabs.sh
spec:
  group: forgelabs.sh
  version: v1alpha1
  scope: Namespaced
  names:
    plural: slashcommands
    singular: slashcommand
    kind: SlashCommand
  validation:
    openAPIV3Schema:
      required: [spec]
      properties:
        spec:
          type: object
          required: [type]
          properties:
            type:
              type: string
              enum: [deploy]