postgresql:
  postgresqlPassword: {{ derivePassword 1 "maximum" .Values.master_password "postgres" "chartmart.com" }}

secrets:
  jwt: {{ derivePassword 1 "maximum" .Values.master_password "jwt" "chartmart.com" }}
  erlang: {{ derivePassword 1 "maximum" .Values.master_password "erlang" "chartmart.com" }}

admin:
  enabled: true
  name: {{ .Values.admin_name }}
  email: {{ .Values.admin_email }}
  password: {{ derivePassword 1 "maximum" .Values.master_password "admin" "chartmart.com" }}
  publisher: {{ .Values.publisher }}
  publisher_description: {{ .Values.publisher_description }}

ingress:
  dns: {{ .Values.chartmart_dns }}

api:
  bucket: {{ .Values.assets_bucket }}

chartmuseum:
  bucket: {{ .Values.chartmuseum_bucket }}
