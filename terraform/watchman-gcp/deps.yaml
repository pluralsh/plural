dependencies:
- name: gcp-bootstrap
  repo: chartmart
  type: terraform
- name: kube
  repo: chartmart
  type: terraform

providers:
- gcp

wirings:
  terraform:
    cluster_name: gcp-bootstrap.cluster_name
    node_pool: gcp-bootstrap.node_pool
    chartmart_namespace: kube.namespace