replicaCount: 2

image:
  repository: dkr.piazzaapp.com/chartmart/chartmart
  tag: '0.1.0'
  pullPolicy: Always

www:
  image: dkr.piazzaapp.com/chartmart/chartmart-www
  tag: '0.1.0'
  port: 80

imagePullSecrets:
- name: chartmartcreds

api:
  port: 4000
  bucket: chartmart-assets

dbPasswordSecret: chartmart-postgresql

chartmuseum:
  repository: chartmuseum/chartmuseum
  tag: latest
  bucket: chartmart-charts

registry:
  replicaCount: 1
  podAnnotations: {}
  podLabels: {}
  podDisruptionBudget: {}
  nodeSelector: {}
  tolerations: []

  persistence:
    accessMode: ReadWriteOnce
    enabled: false
    size: 10Gi

  securityContext:
    enabled: false
    runAsUser: 1000
    fsGroup: 1000

  image:
    repository: registry
    tag: 2.7.1
  secrets:
    haSharedSecret: ""

  configData:
    version: 0.1
    notifications:
      endpoints:
      - name: chartmart
        url: http://chartmart-api:4000/dkr/callback
        timeout: 500ms
        threshold: 5
        backoff: 1s
    log:
      fields:
        service: registry
    storage:
      cache:
        blobdescriptor: inmemory
    prometheus:
      enabled: true
    storage:
      gcs:
        keyfile: /etc/docker/chartmart/gcp.json
    auth:
      token:
        realm: mart.piazzaapp.com/dkr
        service: mart.piazzaapp.com/dkr
        issuer: mart.piazzaapp.com
        rootcertbundle: /etc/docker/registry/cert.pem
    http:
      addr: :5000
      headers:
        X-Content-Type-Options: [nosniff]
    health:
      storagedriver:
        enabled: true
        interval: 10s
        threshold: 3

postgresql:
  postgresqlUsername: chartmart
  postgresqlDatabase: chartmart

nameOverride: ""
fullnameOverride: ""

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  dns: mart.piazzaapp.com
  dkr_dns: dkr.piazzaapp.com
  annotations:
    networking.gke.io/managed-certificates: chartmart-cert,chartmart-dkr-cert

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
